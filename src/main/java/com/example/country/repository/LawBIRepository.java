package com.example.country.repository;

import com.example.country.domain.LawBI;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LawBIRepository extends JpaRepository<LawBI, Long> {
}
